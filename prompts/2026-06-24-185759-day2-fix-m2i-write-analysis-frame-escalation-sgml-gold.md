# Day 2 — Fix m2i 0.0, Write ANALYSIS.md (Per-Bucket + Verify-Without-Truth), Frame Escalation, + Independent SGML Gold

- **Datetime:** 2026-06-24 18:57:59 (Asia/Taipei, UTC+8)
- **Summary:** Day 2, the B->A move: the repo had no analysis deliverable and the escalation tier is a stub (both graded directly). Three things in order, no optimization/autoresearch loop: (1) fix-or-disclose the live m2i boundary-0.0 case, (2) write ANALYSIS.md with real per-bucket numbers + the verify-without-public-ground-truth section + an honest failure list, (3) frame the escalation stub as a defended cost cut. Then optionally hand-label 1-2 NEW non-iXBRL boundary gold filings independently.

## Prompt

> day 2 for the 10k repo ... this is where the B-to-A grade is, because it has no analysis doc and the escalation tier is a stub ... three things in order. do not run any optimization or autoresearch loop.
> first, the live failing case. m2i scores boundary 0.0 because its header is "Item\n1." ... trace it first to confirm that's actually the cause, then decide: either a small regex change ... or ... disclose it as a named SGML/odd-header limitation. the one thing you can't do is leave a goldset filing at 0.0 while the repo claims "robust under format variance".
> second, the biggest grade move: there is no ANALYSIS.md ... write one, with real numbers from the audited gold: [boundary cross-check + char-gold per bucket, each era separately with N, never pooled] [cost per filing] [scalability + how you verify WITHOUT public ground truth, named by test files] [honest still-difficult/unsupported with concrete filings].
> third, the escalation tier. keep the DeferredLLMClient stub, but in ANALYSIS.md frame it as a deliberate, defended cost-discipline cut ... deferral ledger ... estimated cost versus adjudicate-all.
> only if real time remains ... hand-label 1-2 NEW boundary gold filings ... independently ... on a non-iXBRL era ... msft-fy1995 (SGML) or GE ... tag them human-labeled and audit each snippet. /save-prompt

## What I Did

- **#1 m2i traced + fixed (commit cfd889e).** Confirmed the cause: m2i iXBRL renders every token on its own line, so headers arrive as `Item\n1.` and the regex `item[ \t]+\d` matched nothing -> 0 items -> boundary 0.0. Widened the item/number gap to tolerate a SINGLE newline (still rejecting `Item1.` and blank-line gaps, with unit tests for both). m2i now segments 23 items whose item-1/1A offsets match its INDEPENDENT (title-labelled) gold exactly -> boundary 0.0 -> **1.0 (2/2)**; full eval unchanged elsewhere; 60 tests pass. A clean fix, not a can of worms.
- **#2 ANALYSIS.md written (commit 895e439).** Real numbers from the audited gold: results **per format bucket, never pooled** (iXBRL boundary 1.0 over 4 gold; SGML/GE called out separately, not averaged in); presence recall 1.0, silent-failure 0/7; cost per filing (deterministic ~$0 at 100%, escalation flagged-only 64/161 slots windowed, Pareto 64 vs 161 calls / -96% on clean, Copilot-quota framing); **verify-without-public-ground-truth** named by file+test (structural/round-trip 7/7 `validate.py`, dual-extractor cross-check `boundary_crosscheck.py` as a PARTIAL gate, char-gold IoU `evalkit.py`, XBRL Item-8 oracle 4/7 `oracle.py`, mutation battery `tests/test_boundary_crosscheck.py`); honest still-difficult list (GE ungoldable, SGML thin, m2i back-half, common-mode partial gate, small-item abstain).
- **#3 escalation framed (ANALYSIS.md §7, in 895e439).** Kept the `DeferredLLMClient` stub; framed as a defended cut: the deterministic path already clears the bar at $0; `run_escalation` records every candidate (`escalation_deferred` provenance + the summary ledger) so nothing is silently skipped; index-don't-generate LIB prompt is cheap/safe when wired; a measurable stub beats an unmeasurable half-built tier.
- **Optional #4 done (commit d30bd27).** Hand-labeled msft-fy1995 (SGML) INDEPENDENTLY: located items 1/2/7 body sections by reading the all-caps body headers (distinct from the dotted-leader TOC), audited each start/end snippet, tagged `human-labeled-independent`. The production extractor matches at IoU 1.0, so the SGML boundary bucket is now MEASURED (1.0, 3/3), not just asserted. Item 8 deliberately left ungolded (statements-after-Item-9 ambiguous layout, disclosed). Boundary gold now 5 filings across 2 eras. Updated ANALYSIS + report wording.

All 4 commits authored `wshung <...noreply...>`; **local, 3 commits ahead of origin/main, NOT pushed** (awaiting permission). No optimization/autoresearch loop was run.
