# Capability Fix: Canonical-Source Robustness + edgartools Fallback Tier (Empty/Collapsed Filings)

- **Datetime:** 2026-06-24 21:25:19 (Asia/Taipei, UTC+8)
- **Summary:** A 12-filing diverse batch (~62% extracted) exposed a robustness hole the 7-filing curated set hid: JPM FY2023 + 374Water FY2025 extracted COMPLETELY EMPTY (23 shells, 0 bodies), GE FY2009 collapsed all bodies into a 314KB Item 8. Priority = a capability fix, not more analysis. Wire edgartools' .obj().items as a FALLBACK tier (regex stays primary, $0 on the common case); keep the char_range contract by locating each edgartools item's text head back in our canonical (text match, not offset copy). Prove on the 3 failures, add JPM+374Water to the eval, confirm gold/tests don't regress, and make ANALYSIS honest about the broad reality. Don't replace the regex, don't over-engineer, don't push.

## Prompt

> i ran a 12-filing diverse batch ... JPM FY2023 ... and 374Water FY2025 both extracted COMPLETELY EMPTY ... GE FY2009 collapsed all bodies into Item 8. ... wire edgartools' structured item parse (.obj().items) as a FALLBACK tier. keep the regex/anchor as primary ... when the deterministic segmenter produces a bad result ... fall back ... locate each edgartools item's text head/tail back in our canonical (text match, not offset copy). verify the round-trip / coverage still holds. then prove it on the exact failures ... add JPM + 374Water to the eval as real test cases ... confirm the gold ... does NOT regress and the 60 tests stay green. last, update ANALYSIS.md ... add the "modern-filing empty extraction (regex) -> edgartools Tier-0 fallback" story with before/after numbers, and don't leave "presence recall 1.0" implying general robustness when a 12-filing batch was ~62%. ... don't push without my say-so /save-prompt

## What I Did

- **Traced each failure to its real root cause (not "the segmenter chokes"):**
  - **JPM FY2023**: edgartools' `filing.text()` returned a 75-char repr stub (`"<class ...>.__repr__ returned empty string"`); the canonical was empty, so the regex had nothing. raw.html was 13MB with the full body. -> **Tier-0 canonical-source fix** (`normalize._source_text`): strip `raw.html` when `.text()` is broken/empty. JPM **0 -> 22 items** (regex then works).
  - **374Water FY2025 (full 10-K)**: token-per-line iXBRL (`Item\n1.`) -> already fixed by today's earlier newline-tolerance commit. **0 -> 21 items**. (Its ticker lookup also returned the Part-III-only 10-K/A; pinned the full 10-K by accession.)
  - **GE FY2009**: regex collapsed (Item 8 = 314KB blob, lead items lost) -> the **edgartools fallback fires** and recovers **16 items**.
- **Implemented `sec10k/fallback.py`**: `needs_fallback` (empty, or one item >50% of doc AND item 1 missing) + `locate_spans` (normalize canonical with an offset map, find each edgartools item's head, map back to a real char offset, tile). Wired in `pipeline._present_items` (regex primary; fallback only if its result is better). Fixed `validate.assess` to PRESERVE the segmentation extractor in provenance (`anchor` vs `edgartools-fallback`) instead of hardcoding `anchor` -- which is how I discovered GE actually uses the fallback.
- **Verified:** round-trip/coverage hold on all 3 (round_trip_ok=True). 3 new fallback tests (unit locate + needs_fallback + pipeline-engages) + a canonical-html-fallback test -> **64 tests pass**. Eval gold **1.0/5 unchanged** (no regress), silent-failure **0/9**. Added jpm-fy2023 + scwo-fy2025 to `eval_set.json` (empty pre-fix, recall 1.0 now).
- **Honest ANALYSIS (§2.5):** pre-fix diverse batch ~62%; post-fix fresh diverse batch **7/9 (~78%)**; relabelled "recall 1.0" as the CURATED set (not general robustness). Before/after table per failure, the fallback design, and residuals (a BAC 10-K still drops lead items; ticker-by-year can pick a 10-K/A).
- Two commits (`d7fb839` code, `d9f75c4` eval+docs); **local, 5 commits ahead of origin/main, NOT pushed**. Regex untouched as primary; fallback is conservative (never fires on the cooperative case, keeps the regex where edgartools is worse, e.g. GE Item 7A parses to 560KB > the doc).
