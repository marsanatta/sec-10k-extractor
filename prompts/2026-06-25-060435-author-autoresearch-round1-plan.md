# Author The First Official Autoresearch Round Plan (Review Doc Only)

- **Datetime:** 2026-06-25 06:04:36
- **Summary:** Kick-off for the first official eval-driven Modify→Verify→Keep/Discard autoresearch round — but author the structural-invariant-guided PLAN as a review document first (no execution, no code), grounded in the iteration playbook and the verification tower.

## Prompt

> this is the kick-off for your first official autoresearch round — an eval-driven Modify→Verify→Keep/Discard loop. DON'T run it yet. first AUTHOR the plan yourself (you can use /autoresearch:plan to structure it), as a review document. i'll read it, and only after it passes will you be asked to execute.
>
> context: an earlier scout run already proved the mechanism works; this official round is the repo's own real iteration from the current clean main as baseline. when it eventually runs it'll be in an isolated worktree branched from main, then reviewed and merged back, never pushed. right now i only want the PLAN.
>
> write a structural-invariant-guided plan — you do NOT have a per-task attribution layer and you don't need one, your verification tower is your targeting system. ground it in your domain brain at [home]/job/homeworks/ITERATION-PLAYBOOK-10k.md (read it), and your own verification tower: the structural invariants (non-empty, plausible coverage, no overlap, monotonic offsets, round-trip exact, starts with a real Item header), the dual-extractor and edgar-corpus decorrelated cross-checks, the char-exact human gold, and the mutation battery.
>
> the plan has to spell out, concretely:
>
> - the CLIMBABLE metric(s), each an INDEPENDENT signal: stratified structural-pass rate (label it explicitly as an UPPER BOUND on robustness, NOT accuracy), gold recall on the human-audited gold, and count of new failure modes found. never self-consiste agreeing with itself, and never leaningon edgartools alone since it's already in the extraction path. say why each is independent.
> - the GUARD: the offline fixture suite green AND network-free (at least 65), regex stays PRIMARY, the fallback stays conservative, and the cha5, NEVER auto-frozen from regex /edgartools / edgar-corpus output. say how you check each.
> - the per-iteration SCOPE — one probe pee candidates (order them by ROI in yourplan): (a) stratified eval growth (era / filer-type / sector / structure, accession-pinned and immutable); (b) the form-type-aware expected-item template fix (10-K/A amendments legitimately lack Item 1, so make expectation() form-aware); (c) a structural sweep over a stratifiedail; (d) close the ESCALATION APPLY-LOOP — this is a real, code-confirmed gap: run_escalation (escalation.py:89-93) calls client.adjudicate(), sums the token cost, but NEVER uses adj.text to update the boundary, so the model's answer (the corted, paid for, and discarded — theescalation tier currently flags candidatnot fix any boundary even if a realprovider were wired, and the existing mock test (test_mock_client_token_ledger_is_summed) only asserts the token ledger sums, not that a boundary mn't closed). the fix: parse adj.text'sline number, map it back to a canonical char offset, update the item's char_range; the real provider can stay deferred, but the apply-step plus its test make the tier genuinely functional instead of decorative. CRITICAL — gate this on an INDEPENDENT s style: the test must feed aDELIBERATELY-WRONG boundary, have a mock client return the CORRECT line, and assert the span actually moves to the correct position checked against the char-exact gold or a known-correct offset — NOT just that tokens accrued. if you can't prove the boundaryainst an independent reference, don'tclaim it works.
> - the KEEP/DISCARD rule: a kept change must improve a real independent signal AND pass the guard, else discard and document.
> - the STOP conditions: the failure tail traces to the named common-mode ceiling (needs a decorrelated non-header / CRF extractor, out of scope), or budget. bound it, around 25 iterations.
> - where findings get written, and that n
>
> i'll review your plan against this bar: is every metric independent (no self-consistency, structural-pass labeled upper-bound-not-accuracy), does the guard protect the offline gate and the human-gold floor, does the escalation-apply candidate gate on a real boundary-correction check (not token accounting), are the stop conditions real, is the scope bounded, and does it document discards honestly. write the plan as a doc, don't execute, don't touch code yet your plan md should be created in the new folder called research in this repo /save-prompt

## What I Did

- **Read the domain brain** `ITERATION-PLAYBOOK-10k.md` and grounded the plan in it (the loop one-liner, the anti-Goodhart precondition, buckets B1–B6, treatments T1–T4, stop at the named ceiling).
- **Confirmed the escalation apply-loop gap line-exactly** in `sec10k/escalation.py:89-93`: `run_escalation` calls `adjudicate()` and sums `adj.input_tokens`/`adj.output_tokens` but never uses `adj.text` to move `char_range`; the mock test asserts only token sums. So candidate (d) is a real, code-confirmed gap.
- **Invoked `/autoresearch:plan`** for structure, then deliberately **diverged** from its wizard: it wants a single mechanical metric + a launch, but the playbook (§0–§1) forbids a single blind metric (Goodhart) and the user wants a review doc, not a launch — so I authored a multi-signal, structural-invariant plan by hand and documented the divergence honestly. No AskUserQuestion (the user pre-specified everything; asking would re-litigate).
- **Wrote `research/autoresearch-round-1-plan.md`** (new `research/` folder), covering: three INDEPENDENT climbable signals (stratified structural-pass labelled UPPER-BOUND-not-accuracy, human-gold recall, new-failure-mode count) each with an independence justification + a forbidden-proxy list (presence recall, needs_review count, edgartools-alone); the GUARD G1–G5 (offline suite green + network-free ≥ 65, regex primary, fallback conservative, char-gold stays 5 never auto-frozen, baseline integrity) each with how it is checked; per-iteration SCOPE ROI-ranked (#1 eval growth, #2 form-type expectation() fix, #3 structural sweep, #4 escalation apply-loop) with the apply-loop gated on an independent inject-wrong → mock-correct → assert-span-moves-to-gold (IoU ≥ 0.9) test, NOT token accounting; KEEP/DISCARD rule; STOP conditions (named common-mode / GE-class ceiling, plateau, ~25-iteration budget); findings location (`research/` + `prompts/`); and a self-check table mapping the plan to the review bar.
- **Did NOT execute and did NOT touch any pipeline code.** Plan is a markdown doc only, awaiting review.
- Saved this prompt (Taipei time per project convention). Not committed (per save-prompt convention — left for the user to batch).
